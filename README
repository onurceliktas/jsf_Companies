Type this command to create your database correctly;

1. create database Corporation;
-------------------------------
2. use corporation;
-------------------------------
3. CREATE TABLE Users(
user_ID INT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
user_name nvarchar(30) NOT NULL,
user_userName nvarchar(20) not null,
user_pw nvarchar(10) not null
);
------------------------------------------------
4. Create TABLE Companies(
company_ID int unsigned auto_increment primary key,
company_name varchar(100),
company_address varchar(100),
company_web varchar(100)
);
------------------------------------------------


If you are using Linux and Netbeans IDE, you need to do following;

- Open project,
- Resolve problems,
- Go to the file where Netbeans is installed,
- ../netbeans-8.2/ide/modules/ext  and then select mysql-connector.
